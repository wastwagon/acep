# Copy to Coolify "Environment Variables" or to `.env` next to docker-compose.coolify.yml
# Never commit real secrets.

# --- Required ---
POSTGRES_PASSWORD=change-me-to-a-long-random-string

# --- CMS admin bootstrap (used by automatic seed on deployment) ---
# Use your production org domain; sign in at https://www.acep.africa/login → /admin for CMS
CMS_ADMIN_EMAIL=admin@acep.africa
CMS_ADMIN_PASSWORD=change-me-to-a-strong-password
CMS_ADMIN_NAME=ACEP Africa Administrator

# --- Public URLs (used at Next.js build time for NEXT_PUBLIC_* and for CORS) ---
# Main site (live): https://www.acep.africa
NEXT_PUBLIC_SITE_URL=https://www.acep.africa
# If API is a separate service, set its public URL; else same origin is fine:
NEXT_PUBLIC_API_URL=https://www.acep.africa

# --- Optional overrides ---
# POSTGRES_USER=acep
# POSTGRES_DB=acep
# WEB_PUBLISH_PORT=3100
# API_PUBLISH_PORT=3001

# --- Optional Redis password ---
# Set REDIS_PASSWORD to enable Redis AUTH. Backend resolves REDIS_URL automatically unless you set REDIS_URL.
# For the Next.js `web` service, when using a password set REDIS_URL explicitly, e.g.:
# REDIS_URL=redis://:your-redis-secret@redis:6379
# (URL-encode special characters in the password.)
